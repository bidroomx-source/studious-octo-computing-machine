'use client';

export default function Home() {
  return (
    <div className="min-h-screen bg-slate-50">
      <h1 className="text-4xl font-bold text-center py-20">BidRoom</h1>
      <p className="text-center">Your reverse storage auction platform is live!</p>
      <p className="text-center mt-4">We can now add all the features (room types, tiers, Whole House, etc.)</p>
    </div>
  );
}